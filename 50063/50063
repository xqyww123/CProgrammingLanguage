
#include "stdio.h"
int main(void)
{
    int i, m, n;
    int repeat, ri;
    int factorsum(int number); 
  
    scanf("%d", &repeat);
    for(ri = 1; ri <= repeat; ri++){
        scanf("%d%d", &m, &n);
	int f = 0;
	for (i=m;i<=n;++i)
	{
		int to = i, j;
		for (j=1;j<i;++j)
			if (!(i%j))
				to -= j;
		if (i == 1) to = 0;
		if (!to)
		{
			if (f) printf(" ");
			f = 1;
			printf("%d",i);
		}
	}
        printf("\n");
    }
}
