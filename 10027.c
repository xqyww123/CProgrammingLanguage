#include <stdio.h>
#include <string.h>
char p[16];
void main()
{
	char	*change(char s[4][20]);
	char	s[4][20];
	int	i;
	for(i=0;i<4;i++)
		scanf("%s",s[i]);
	for (i=0;i<4;++i) { int l = strlen(s[i]); putchar(s[i][l-2]); putchar(s[i][l-1]);}
	putchar('\n');
}
char	*change(char	s[4][20])
{
	int	i,t,len;
/*---------*/
	return (p);
}
